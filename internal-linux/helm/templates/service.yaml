apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  labels:
    app: {{ .Release.Name }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.internalLinux.sshPort }}
      targetPort: {{ .Values.internalLinux.sshPort }}
      protocol: TCP
      name: ssh
  selector:
    app: {{ .Release.Name }}