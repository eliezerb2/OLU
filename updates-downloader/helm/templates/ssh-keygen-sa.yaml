apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.sshKeygenJob.name }}
  labels:
    app: {{ .Release.Name }}
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "-40"